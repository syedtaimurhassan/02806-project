# macOS / editor noise
.DS_Store
**/.DS_Store
.ipynb_checkpoints/
.vscode/

# Python / notebook cache
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local virtual environments
.venv/
venv/
env/

# Raw data and archives — use NYC Open Data instead of committing these.
Parking_Violations_Issued_-_Fiscal_Year_2025.csv
*.zip

# Heavy executed notebook (142 MB with outputs). Commit the stripped copy instead.
analysis.ipynb

# Older draft notebook — superseded by explainer_notebook.ipynb.
analysis_github.ipynb

# Generated/intermediate analysis outputs — not needed by the static site.
outputs/

# Plotly iframe renderer output (created when the notebook runs locally).
iframe_figures/

# Local course notes and drafts not part of the final submission.
social_viz_llm_context.md
trafic draft.rtfd/
project-req.rtf
segal&heer.pdf

# Older root-level scratch files. The final site lives in site/.
/assets/
/styles.css
/script.js

# Lightweight site data used during development only.
site/data/

# Unused optional figure exports — not embedded in the story.
site/figures/fig12_dominant_family_by_precinct.html
site/figures/fig13_street_cleaning_precinct_share.html
